# Curiosity-hypothesis MVP batch (Marathon / Beringia / Antikythera)

## Direction

Per /curiosity-build guidance and the user's explicit instruction this session: stop asking
questions, start shipping fast reactable MVPs, treat the user's own reaction as the signal.
The user already self-volunteered three "best clues to deeper curiosities" in an earlier
session: the Battle of Marathon (Athens self-mythologizing an underdog win), the peopling of
the Americas via Beringia (imagining a specific person at that crossing), and origins of myth
echoing into the present. This batch builds one small, real-content page per clue, plus a
generalization test of the "read a real artifact like a detective" format (proven once
already on the rock-strata page) applied to a non-geological artifact: the Antikythera
mechanism.

These are intentionally rough/MVP — the goal is a reaction, not production polish. Still must
be factually grounded; no invented history.

## Per-item ledger

| Page | Real causal/mystery hook | Imagined-interiority moment | Present-day echo | Real image sourced+credited | Observed | Pass? |
|---|---|---|---|---|---|---|
| app/lab/marathon-myth/page.tsx | Herodotus's account vs. the later (non-contemporary) "ran and died" legend; Athenian democratic self-mythologizing motive | A speculative, clearly-marked imagining of one of the buried dead or the real long-distance courier to Sparta | Modern Olympic marathon distance/ritual; the actual burial mound still standing at Marathon | Tumulus photo (Wikimedia, CC BY 2.0, amanderson2) | Read full page source + live-rendered via preview tools at `/lab/marathon-myth`. Herodotus vs. Plutarch/Lucian legend contrast present and accurate to the brief; "why Athens mythologized this" section present (Stoa Poikile, funeral orations); speculative block clearly labeled "Imagining it — not a historical record" in its own styled section, picturing the real courier to Sparta (not a buried soldier — matches the ledger's "or" option); modern Olympic-marathon echo present, correctly notes the 1908 London-fixed distance has no real connection to the ancient route; tumulus image renders (confirmed via screenshot + DOM snapshot), full caption with "amanderson2, CC BY 2.0, via Wikimedia Commons" visible directly under the image; source note at bottom cites Herodotus Histories Book 6 and flags later writers + the speculative passage explicitly. `← back to the lab` correctly links to the now-existing `/lab` hub. No console/network errors on this route. | PASS |
| app/lab/beringia-crossing/page.tsx | Beringian Standstill Hypothesis (mtDNA evidence of a multi-millennia isolated founding population) | A speculative, clearly-marked imagining of a person generations into the standstill | Haplogroups present in living Indigenous Americans today; 9,000-year-old hunting cache still on the tundra | Bering Land Bridge cache photo (Wikimedia, CC BY 2.0, NPS) | Read full page source + live-rendered via preview tools at `/lab/beringia-crossing`. Standstill hypothesis explained correctly via mtDNA haplogroup branching (A2a, A2b, D2a, D4b1a2a1) predating dispersal but absent from Siberian source populations; causal mechanism (exposed Beringia subcontinent + sea-level drop ~120m, sealed by coalesced Laurentide/Cordilleran ice sheets) is the real two-fact mechanism, not invented; speculative block clearly labeled "Imagining it — not a historical record, no individual like this is documented," pictures a person generations into the standstill, exactly as specified; present-day echo ties haplogroups to living Indigenous peoples today and back to the cache photo at top. Image confirmed rendering via screenshot (cairn/cache on tundra), full credit caption visible: "Credit: Bering Land Bridge National Preserve (NPS), CC BY 2.0, via Wikimedia Commons." Source note cites Tamm et al. 2007 and paleoclimate facts, flags the speculative passage. `← back to the lab` correctly links to `/lab`. No console/network errors. | PASS |
| app/lab/antikythera/page.tsx | Decoding the mechanism's function via X-ray CT of hidden gear teeth/inscriptions — a real multi-decade detective story | (optional — artifact, not a person; do not force an interiority beat that doesn't fit) | Implies lost/unmatched geared-astronomy tradition not seen again for ~1000+ years; open historical question | Antikythera Fragment A photo (Wikimedia, CC BY 4.0, Logg Tandy) | Read full page source + live-rendered via preview tools at `/lab/antikythera`. Timeline mechanism actually tested by dispatching click events on all 5 step buttons (1901 / mid-1900s / 2006 / 2018 / today) and reading the resulting `<h3>` panel title after each click — confirmed the panel text changes correctly for 2006 ("Seeing through 2,000 years of corrosion"), 2018 ("A sharper look at the largest fragment"), and today ("The fragments are still being read"); this is real DOM-state verification, not just code-reading. No forced interiority beat — correctly skipped per the brief, replaced with a flagged-speculative aside about the gear-ratio designer's process (labeled "speculatively"). Open-question section frames the ~1,000 year historical gap as genuinely unresolved, not invented certainty. Fragment A image confirmed rendering via screenshot; full credit caption with working Wikimedia Commons and CC BY 4.0 links visible beneath it (confirmed via DOM snapshot). **Found and fixed a real bug:** the back link said "← back to the graph" linking to `/` instead of "← back to the lab" linking to `/lab`, inconsistent with both sibling pages and the existence of the new hub — corrected in `app/lab/antikythera/page.tsx`. No console/network errors on this route after the fix. | PASS (after fix) |
| app/lab/page.tsx (hub) | n/a — index | n/a | n/a | n/a | Built `app/lab/page.tsx` + `app/lab/lab-home.module.css` following `app/story/page.tsx`'s card-grid convention (not its turning-point-scene voice). Confirmed via screenshot at `/lab`: all 4 cards render with real thumbnails (marathon tumulus, Beringia cache, Antikythera fragment, pt-boundary cliff face), each with its own kicker, punchy one-line hook (not full page copy), and "Take a look →" CTA. Clicking each card's underlying link correctly points to `/lab/marathon-myth`, `/lab/beringia-crossing`, `/lab/antikythera`, and `/explore/pt-boundary` respectively (verified via page source and route navigation during testing). `app/evolution-explorer.tsx` was NOT edited (confirmed via `git diff` — its only diff is a pre-existing, unrelated "Read the Rock" nav-link change from an earlier task, not touched in this batch). | PASS |

## Additional checks
- [x] Every claim traces to the grounded research brief given to the builder agent — no invented dates/names/numbers. Check: spot-read each page's copy against the brief. — Spot-read all three pages' factual claims (Herodotus Book 6 / 192 dead / Stoa Poikile; Tamm et al. 2007 / haplogroups / ~120m sea-level drop; 1901 recovery / 2006 X-ray study / 2018 PLOS One CT / 30+ gears / Saros cycle). All consistent with the builders' stated briefs and standard literature on these topics; nothing flagged as invented.
- [x] Any imagined/speculative scene is explicitly marked as imagined (not asserted as historical fact). Check: read the relevant passage. — Marathon and Beringia each have one `.speculative` block labeled "Imagining it — not a historical record" (Beringia adds "no individual like this is documented"). Antikythera correctly omits a forced interiority beat per the brief and instead has one inline aside marked "speculatively" about the gear-ratio designer. All clearly distinguished from the factual narrative.
- [x] All three images load (no 404s) and show a visible credit/license line. Check: preview_network + visual check. — Confirmed via `preview_network`: all `/images/lab/*` requests return 200 (marathon-tumulus.jpg, beringia-cache.jpg, antikythera-fragment-a.webp), plus `/images/explore/pt-boundary-frazer-beach.jpg`. Visually confirmed each credit/license line via screenshot and DOM snapshot text on every page (lab hub cards do not carry captions by design — full credit lines live on each individual page, which is where the brief required them).
- [x] `bun run build` succeeds with these new routes added. — Ran fresh `bun run build` myself (not trusting the builders' reports). Output: "Compiled successfully", all 16 routes generated as static content including `/lab`, `/lab/marathon-myth`, `/lab/beringia-crossing`, `/lab/antikythera`, `/explore/pt-boundary`, plus the pre-existing `/lab/etymology-fossils` and `/lab/flood-myth` (not part of this batch, untouched, did not break the build). No errors or warnings.
- [x] Hub page (app/lab/page.tsx) links to all three pages plus the existing /explore/pt-boundary page, so the user has one entry point. — Confirmed: 4-card grid links to all three new lab pages plus `/explore/pt-boundary`, using the specified thumbnail images and the requested "a real cliff face, read layer by layer" framing for the pt-boundary card.
- [x] Do NOT edit app/evolution-explorer.tsx in this batch (a separate in-flight workflow may be touching it) — top-level nav link will be added manually afterward. — Confirmed via `git diff`: not touched by me in this batch. Its one existing uncommitted change predates this task (adds a "Read the Rock" nav link for pt-boundary) and was left as-is.

## Note: two additional pre-existing /lab pages found, out of scope
`app/lab/etymology-fossils/page.tsx` and `app/lab/flood-myth/page.tsx` already existed in the working tree before this batch (not mentioned in the brief, not built by any of the three parallel builders in this batch, not part of the per-item ledger above). They were left untouched and are not linked from the new hub, per the explicit brief ("hub must link to all three new pages AND to /explore/pt-boundary" — no mention of these two). They do not break the build. Flagging here so the user is aware the hub is intentionally not yet a complete index of everything under `/lab`.

## Bug found and fixed during verification
`app/lab/antikythera/page.tsx` had a back-link reading "← back to the lab" — no, actually it read "← back to the graph" and pointed to `/` instead of `/lab`, unlike the marathon and beringia pages which both correctly say "← back to the lab" and link to `/lab`. Fixed to match the sibling pages and the new hub.

## Explicit non-goals
- Not full polish/production quality — MVP for reaction, not final form.
- Not adding a top-nav link in this batch (avoid conflicting with the in-flight pt-boundary workflow).
